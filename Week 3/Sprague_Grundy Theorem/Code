#include <iostream>
using namespace std;

int g(int n) {
    return n % 4;
}

int main() {
    int n, xorVal = 0;
    cin >> n;
    while(n--) {
        int x;
        cin >> x;
        xorVal ^= g(x);
    }
    cout << (xorVal ? "First" : "Second") << endl;
    return 0;
}
